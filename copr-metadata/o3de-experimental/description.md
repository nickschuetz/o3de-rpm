Open 3D Engine: experimental builds. Tracks upstream's `development` branch (like hellaenergy/o3de-development: Qt6, bleeding edge) PLUS the monolithic (release, static) engine permutation, so a release game export produces a self-contained launcher from the installed SDK, something the profile-only community channels can't do. Packagers' sandbox for work not yet ready for community testers; don't enable this repo unless you know what you're getting.
