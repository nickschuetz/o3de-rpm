
# Python bytecode from tools/
tools/__pycache__/

# Claude Code per-checkout state (scheduled-task locks, local settings, etc.)
.claude/

# O3DE launcher run-time artifacts written into CWD when a launcher
# is invoked from this directory (backtrace stubs, imgui window state).
# Not relevant to the packaging work itself.
backtrace.log
imgui.ini

# Working drafts for upstream posts/patches. These are local scratch and are
# deliberately NOT published to this public repo (committing them, even
# briefly, would put them in public history permanently). The single
# intentionally-published doc is un-ignored below.
upstream-drafts/
!upstream-drafts/bundling-exception-libtiff.md
